import SimpleImageEditor from './components/index.vue';

// const ImageEditorPlugin = {
//   install: (app) => {
//     app.component('ImageEditor', SimpleImageEditor);
//   }
// };

// export default ImageEditorPlugin; // 默认导出插件对象

export { SimpleImageEditor };